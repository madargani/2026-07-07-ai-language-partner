---
phase: 01-foundation-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - biome.json
  - src/index.ts
  - src/client.ts
  - src/lib/config.ts
  - src/lib/prisma.ts
  - src/lib/languages.ts
  - src/types/discord.ts
  - src/commands/index.ts
  - src/commands/ping.ts
  - src/commands/setup.ts
  - src/commands/new.ts
  - src/events/ready.ts
  - src/events/interactionCreate.ts
  - src/deploy-commands.ts
  - vitest.config.ts
  - src/__tests__/setup.ts
  - src/__tests__/walking-skeleton.test.ts
  - prisma/schema.prisma
  - Dockerfile
  - Dockerfile.dev
  - docker-compose.yml
  - docker-compose.dev.yml
  - .env.example
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - INFRA-01
  - INFRA-02
  - INFRA-04
  - INFRA-05
user_setup:
  - service: "Discord Developer Portal"
    why: "Bot token and privileged gateway intents — agent cannot create Discord applications"
    env_vars:
      - name: DISCORD_TOKEN
        source: "Discord Developer Portal → Bot → Reset Token"
      - name: DISCORD_CLIENT_ID
        source: "Discord Developer Portal → OAuth2 → General → Client ID"
      - name: DISCORD_GUILD_ID
        source: "Discord → Server Settings → Widget → Server ID (enable Developer Mode)"
    dashboard_config:
      - task: "Create a new application at https://discord.com/developers/applications"
        location: "Discord Developer Portal"
      - task: "Go to Bot settings, click 'Reset Token' and copy the token"
        location: "Bot → Token"
      - task: "Enable Privileged Gateway Intents: SERVER_MEMBERS_INTENT, MESSAGE_CONTENT_INTENT"
        location: "Bot → Privileged Gateway Intents"
      - task: "Invite bot to a development server with scopes: bot, applications.commands"
        location: "OAuth2 → URL Generator → SCOPES: bot, applications.commands → BOT PERMISSIONS: Send Messages, Use Slash Commands, Embed Links, Read Message History"
  - service: "Docker Engine"
    why: "Required for Docker Compose orchestration"
    env_vars: []
    dashboard_config:
      - task: "Ensure Docker Engine 24+ and Compose V2 plugin are installed"
        location: "Terminal — `docker --version && docker compose version`"
must_haves:
  truths:
    - "User can run /setup in a Discord server and select native + target language from dropdown menus"
    - "User's language settings persist across bot restarts (verified by restarting container)"
    - "First-time user running /new sees an error telling them to run /setup first"
    - "All slash commands respond within 3 seconds using deferReply()"
    - "All three services (bot, PostgreSQL, Redis) start healthy with a single `docker compose up`"
    - "Bot shuts down cleanly on SIGTERM/SIGINT — disconnects Discord client, disconnects Prisma"
  artifacts:
    - path: "src/index.ts"
      provides: "Application entry point with config validation, client setup, event wiring, command deployment, and graceful shutdown"
      min_lines: 60
    - path: "src/lib/config.ts"
      provides: "Zod-validated environment configuration"
      exports: ["env"]
      contains: "DISCORD_TOKEN"
    - path: "src/lib/prisma.ts"
      provides: "Singleton PrismaClient for database access"
      exports: ["prisma"]
      pattern: "globalThis"
    - path: "src/commands/setup.ts"
      provides: "/setup command with two-step select menu flow"
      contains: "StringSelectMenuBuilder"
    - path: "src/commands/new.ts"
      provides: "/new command with configured check"
      pattern: "configured"
    - path: "prisma/schema.prisma"
      provides: "User model with discordId, nativeLanguage, targetLanguage, configured flag"
      contains: "model User"
    - path: "docker-compose.yml"
      provides: "Docker Compose base config with postgres, redis, and bot services"
      contains: "condition: service_healthy"
    - path: "docker-compose.dev.yml"
      provides: "Development overrides with tsx watch hot-reload"
      contains: "tsx watch"
  key_links:
    - from: "Discord API"
      to: "src/events/interactionCreate.ts"
      via: "WebSocket gateway — client.on('interactionCreate') dispatches to command handlers"
      pattern: "interactionCreate"
    - from: "src/commands/setup.ts"
      to: "prisma/schema.prisma"
      via: "prisma.user.upsert() saves language selections to PostgreSQL"
      pattern: "prisma\\.user\\.upsert"
    - from: "src/deploy-commands.ts"
      to: "src/commands/index.ts"
      via: "Barrel import of all command builders, registered via REST API v10"
      pattern: "Routes\\.application"
    - from: "docker-compose.yml"
      to: "src/index.ts"
      via: "depends_on condition: service_healthy waits for PostgreSQL before bot starts"
      pattern: "depends_on"

---

<objective>
**As a** language learner, **I want to** configure my native and target languages that persist across sessions, **so that** I can start practicing without re-entering my preferences every time.

**Note:** The ROADMAP.md goal is not in user-story format. This user story is derived from requirements context. Run `/gsd mvp-phase 01` if ROADMAP.md target format is desired.

Purpose: Deliver the operational Walking Skeleton — a Discord bot that connects, registers slash commands, persists user language configuration via Prisma/PostgreSQL, and deploys via Docker Compose alongside PostgreSQL and Redis. This is the thinnest end-to-end slice proving the full stack works.

Output:
- A working Discord bot with /ping, /setup, and /new slash commands
- PostgreSQL User table with language preferences
- Docker Compose configuration for bot, PostgreSQL, and Redis
- Automated test suite validating command structure, config validation, Prisma connectivity
- SKELETON.md recording architectural decisions for all subsequent phases
</objective>

<execution_context>
@/home/madargani/Work/tries/2026-07-07-ai-language-partner/.opencode/gsd-core/workflows/execute-plan.md
@/home/madargani/Work/tries/2026-07-07-ai-language-partner/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
@.planning/phases/01-foundation-setup/01-VALIDATION.md
</context>

<tasks>

<task id="01-01-01" type="auto" tdd="true" wave="1">
  <name>Task 1: Create test infrastructure and failing integration tests for Walking Skeleton</name>

  <files>
    - `vitest.config.ts`
    - `src/__tests__/setup.ts`
    - `src/__tests__/walking-skeleton.test.ts`
  </files>

  <behavior>
    - Test 1 (config validation): envSchema with all required vars passes; missing DISCORD_TOKEN fails; invalid DATABASE_URL fails; optional NODE_ENV defaults to 'development'
    - Test 2 (Prisma singleton): importing prisma from lib/prisma returns a PrismaClient instance; globalThis pattern works
    - Test 3 (command structure): each command in commands/index exports `data` (SlashCommandBuilder) and `execute` (Function); every execute calls deferReply as first async operation
    - Test 4 (graceful shutdown): shutdown() function exists, attempts client.destroy() and prisma.$disconnect()
    - Test 5 (/setup flow): mock interaction, prisma.user.upsert is called with discordId, nativeLanguage (ISO 639-1), targetLanguage (ISO 639-1), configured=true
    - Test 6 (/new flow): mock unconfigured user lookup → execute returns error message telling user to run /setup
  </behavior>

  <action>
    Create the test infrastructure and write failing tests that define the Walking Skeleton contract. Per D-16/D-17 (Zod env validation per CONTEXT.md), per D-09/D-12 (User model fields), per D-10 (ISO 639-1 codes), per D-05/D-07 (setup UX), per D-08 (/new error message).

    1. Create `vitest.config.ts`:
       - Use `defineConfig` from `vitest/config`
       - Root test directory: `src/__tests__`
       - Environment: `node`
       - TypeScript support via `vite` (tsx handles it)
       - Reporter: `verbose`
       - No coverage for now (add in later phase)

    2. Create `src/__tests__/setup.ts`:
       - Mock PrismaClient: export a mock with `user.findUnique`, `user.upsert` as vi.fn() returning Promise<null>
       - Mock discord.js classes: Client, SlashCommandBuilder, CommandInteraction (mock deferReply, editReply, user.id)
       - Set process.env for tests: DISCORD_TOKEN='test-token', DATABASE_URL='postgresql://test:test@localhost:5432/test', REDIS_URL='redis://localhost:6379'
       - Clean up after each test (vi.clearAllMocks())

    3. Create `src/__tests__/walking-skeleton.test.ts`:
       - Test config validation (6 cases):
         a. All required env vars present → parses successfully
         b. Missing DISCORD_TOKEN → throws ZodError
         c. Invalid DATABASE_URL → throws ZodError
         d. NODE_ENV defaults to 'development'
         e. Schema handles REDIS_URL missing (should fail per D-17)
         f. Empty DISCORD_TOKEN string → throws ZodError (min(1))
       - Test Prisma singleton:
         a. Import prisma → is instance of PrismaClient
       - Test command structure (per D-03):
         a. Import commands from commands/index → each has `data` property
         b. Each command's data has name and description
         c. Each command has `execute` function
         d. Mock execute call — verify deferReply is called
       - Test graceful shutdown (per D-13, D-14):
         a. Import shutdown function from index
         b. Mock client.destroy and prisma.$disconnect
         c. Call shutdown → verify both called
         d. Verify isShuttingDown flag prevents double-call
       - Test /setup command (per D-05, D-06, D-09, D-10):
         a. Mock CommandInteraction with user.id = 'test-user'
         b. Import setup command
         c. Call execute with mock — verify deferReply called
         d. Verify it sends StringSelectMenu (ActionRow with StringSelectMenuBuilder)
         e. Verify prisma.user.upsert called with correct shape: { discordId, nativeLanguage (ISO code), targetLanguage (ISO code), configured: true }
       - Test /new command (per D-08):
         a. Mock CommandInteraction with user.id = 'test-user'
         b. Mock prisma.user.findUnique → null (user not configured)
         c. Verify execute returns error message containing "setup"

    All tests must FAIL initially because no implementation exists. Run `npx vitest run --reporter=verbose` to confirm.
  </action>

  <verify>
    <automated>`npx vitest run --reporter=verbose` — tests exist and produce FAIL results with clear "module not found" or similar errors indicating missing implementation</automated>
    <automated>`grep -c 'describe\|it(' src/__tests__/walking-skeleton.test.ts` — test file has at least 10 test cases covering all 6 behavior areas</automated>
  </verify>

  <done>
    - vitest.config.ts exists with correct configuration
    - Test setup file exists with mocks for PrismaClient and discord.js
    - Test file has test suites covering config validation, Prisma singleton, command structure, graceful shutdown, /setup flow, and /new flow
    - `npx vitest run` reports FAIL with all tests failing due to missing implementation
  </done>
</task>

<task id="01-01-02" type="auto" wave="1">
  <name>Task 2: Implement Walking Skeleton — project scaffold, Prisma, /ping, /setup, /new</name>

  <files>
    - `package.json`
    - `tsconfig.json`
    - `.gitignore`
    - `biome.json`
    - `prisma/schema.prisma`
    - `src/lib/config.ts`
    - `src/lib/prisma.ts`
    - `src/lib/languages.ts`
    - `src/types/discord.ts`
    - `src/client.ts`
    - `src/commands/index.ts`
    - `src/commands/ping.ts`
    - `src/commands/setup.ts`
    - `src/commands/new.ts`
    - `src/events/ready.ts`
    - `src/events/interactionCreate.ts`
    - `src/deploy-commands.ts`
    - `src/index.ts`
  </files>

  <action>
    Implement the full Walking Skeleton that makes all tests from Task 1 pass. Follow project structure decisions: source tree organized by type with src/commands/ (per D-01), shared utilities in src/lib/ (per D-02), commands as single files with SlashCommandBuilder + execute() in one file (per D-03), and a single tsconfig.json with no composite projects (per D-04). Create the following files in order:

    **1. `package.json`:**
    - name: "language-partner-bot", version: "1.0.0", type: "module"
    - scripts: "dev": "tsx watch src/index.ts", "build": "tsc", "start": "node dist/index.js", "test": "vitest run", "db:generate": "prisma generate", "db:push": "prisma db push", "db:migrate": "prisma migrate dev", "deploy:commands": "tsx src/deploy-commands.ts", "lint": "biome check src/"
    - dependencies: "discord.js": "^14.26.0", "@prisma/client": "^6.19.0", "zod": "^4.4.0", "dotenv": "^17.4.0"
    - devDependencies: "prisma": "^6.19.0", "typescript": "~5.9.0", "tsx": "^4.23.0", "vitest": "^4.1.0", "@types/node": "latest", "@biomejs/biome": "latest"
    - After writing, run: `npm install` then `npx prisma generate`

    **2. `tsconfig.json`:**
    - target: "ES2022", module: "NodeNext", moduleResolution: "NodeNext"
    - outDir: "dist", rootDir: "src"
    - strict: true, esModuleInterop: true, skipLibCheck: true, forceConsistentCasingInFileNames: true
    - include: ["src"]

    **3. `.gitignore`:**
    - node_modules/, dist/, .env, prisma/migrations/

    **4. `biome.json`:**
    - Formatter: enabled, indentStyle: space, indentWidth: 2
    - Linter: enabled, rules: { recommended: true }
    - Files: include: ["src/**/*.ts"]

    **5. `prisma/schema.prisma`:** (per D-09 through D-12)
    - generator client: "prisma-client-js"
    - datasource db: "postgresql", url: env("DATABASE_URL")
    - model User with fields:
      - id: String @id @default(uuid())
      - discordId: String @unique
      - nativeLanguage: String (ISO 639-1 code per D-10, string per D-11)
      - targetLanguage: String (ISO 639-1 code per D-10, string per D-11)
      - configured: Boolean @default(false) (explicit flag per D-12)
      - createdAt: DateTime @default(now())
      - updatedAt: DateTime @updatedAt
    - After writing, run: `npx prisma generate`

    **6. `src/lib/config.ts`:** (per D-16, D-17)
    - Zod schema: DISCORD_TOKEN (string min(1)), DATABASE_URL (string url()), REDIS_URL (string url()), NODE_ENV (z.enum(['development','production','test']).default('development'))
    - Parse process.env with schema, crash on failure with formatted ZodError
    - Export typed `env` constant

    **7. `src/lib/prisma.ts`:**
    - Singleton PrismaClient using globalThis pattern
    - Log: ['query', 'warn', 'error'] in development, ['error'] in production
    - Export `prisma` instance

    **8. `src/lib/languages.ts`:** (per D-07)
    - Export const LANGUAGES array of 20 common languages with ISO 639-1 codes:
      en, es, fr, de, it, pt, ru, ja, ko, zh, ar, hi, nl, sv, no, da, fi, pl, tr, th
    - Each entry: { code: string, label: string } (e.g., { code: 'en', label: 'English' })

    **9. `src/types/discord.ts`:**
    - Export Command interface: { data: SlashCommandBuilder | SlashCommandSubcommandsOnlyBuilder, execute: (interaction: CommandInteraction) => Promise<void> }

    **10. `src/client.ts`:**
    - Create and export Client with intents: GatewayIntentBits.Guilds, GatewayIntentBits.GuildMessages, GatewayIntentBits.MessageContent
    - Per RESEARCH.md Pitfall 1 (missing intents = silent failure)

    **11. `src/commands/index.ts`:**
    - Barrel export: import ping, setup, new command modules as Command[]
    - Export `commands` array containing all three command objects

    **12. `src/commands/ping.ts`:**
    - SlashCommandBuilder with name 'ping', description 'Check bot latency'
    - execute: deferReply(), calculate latency from Date.now() - interaction.createdTimestamp, editReply with "Pong! 🏓 Latency: {latency}ms"

    **13. `src/commands/setup.ts`:** (per D-05, D-06, D-07, D-10)
    - SlashCommandBuilder with name 'setup', description 'Configure your native and target languages'
    - execute: deferReply({ flags: MessageFlags.Ephemeral })
    - Two-step select menu flow using `awaitMessageComponent` collector:
      a. Send first menu with LANGUAGES options for native language (customId: 'native_lang')
      b. awaitMessageComponent with filter on customId + user.id, 60s timeout
      c. deferUpdate() on selection, extract nativeLang value
      d. Send second menu with LANGUAGES filtered to exclude nativeLang (customId: 'target_lang')
      e. awaitMessageComponent similar pattern, deferUpdate()
      f. prisma.user.upsert({ where: { discordId }, update: { nativeLanguage, targetLanguage, configured: true }, create: { discordId, nativeLanguage, targetLanguage, configured: true } })
      g. editReply with confirmation: "✅ Configured! Native: {nativeLabel} → Target: {targetLabel}"
      h. Handle timeout (no selection in 60s) with editReply timeout message
    - Import LANGUAGES from lib/languages.ts
    - Per INFRA-04: deferReply() is first async call

    **14. `src/commands/new.ts`:** (per D-08)
    - SlashCommandBuilder with name 'new', description 'Start a new conversation session'
    - execute: deferReply({ flags: MessageFlags.Ephemeral })
    - Look up user: prisma.user.findUnique({ where: { discordId: interaction.user.id } })
    - If !user?.configured: editReply with "⚠️ You need to configure your languages first! Please run `/setup` to set your native and target languages."
    - If configured: editReply with "📝 Conversation sessions are coming in Phase 2! You're all configured — your target language is ready."
    - Per INFRA-04: deferReply() is first async call

    **15. `src/events/ready.ts`:**
    - client.on('ready', ...): log "Logged in as {bot.tag}" and "Serving {guilds.size} guilds"
    - Log the commands that were registered

    **16. `src/events/interactionCreate.ts`:**
    - client.on('interactionCreate', ...): if interaction.isChatInputCommand(), look up command from Map<name, Command>, execute it
    - Catch errors, log to console, if interaction is not replied/deferred, send error reply
    - Map is populated from commands/index.ts at startup

    **17. `src/deploy-commands.ts`:**
    - Import commands from commands/index.ts barrel
    - Build REST client with DISCORD_TOKEN using discord.js REST (v10)
    - Convert commands to JSON via .data.toJSON()
    - If NODE_ENV === 'development': Routes.applicationGuildCommands(DISCORD_CLIENT_ID, DISCORD_GUILD_ID)
    - Else: Routes.applicationCommands(DISCORD_CLIENT_ID)
    - Log success with command count
    - Per Pitfall 3 (RESEARCH.md): guild-scoped for dev, global for prod

    **18. `src/index.ts`:**
    - Top: import 'dotenv/config' (loads .env), import { env } from './lib/config'
    - Import client, register ready + interactionCreate event handlers
    - Define shutdown(signal) function (per D-13, D-14):
      a. isShuttingDown guard flag
      b. Log signal receipt
      c. Set 10s force-exit timeout
      d. await client.destroy()
      e. await prisma.$disconnect()
      f. Clear timeout, process.exit(0)
      g. Catch errors, process.exit(1)
    - process.on('SIGTERM', () => shutdown('SIGTERM')), process.on('SIGINT', () => shutdown('SIGINT'))
    - await deployCommands() before login (or call it standalone)
    - await client.login(env.DISCORD_TOKEN)

    **After creating all files:**
    1. Run `npm install`
    2. Run `npx prisma generate`
    3. Run `npx vitest run --reporter=verbose` — ALL tests must pass (green)
    4. Run `npx tsc --noEmit` — TypeScript compiles without errors
    5. Run `npx biome check src/` — no lint errors

    If any test fails, fix the implementation until green. Do not modify tests to make them pass — the tests define the contract.
  </action>

  <verify>
    <automated>`npx vitest run --reporter=verbose` — all tests pass (green)</automated>
    <automated>`npx tsc --noEmit` — zero TypeScript compilation errors</automated>
    <automated>`npx prisma validate` — Prisma schema is valid</automated>
    <automated>`npx biome check src/` — zero lint errors</automated>
  </verify>

  <done>
    - Full project structure exists with all source files
    - All dependencies installed via npm
    - Prisma client generated successfully
    - All 6 test suites from Task 1 pass
    - TypeScript compiles with no errors
    - Biome lint passes with no errors
    - Bot can start locally with `npx tsx src/index.ts` (requires DISCORD_TOKEN env var)
  </done>
</task>

<task id="01-01-03" type="checkpoint:human-verify" gate="blocking" wave="1">
  <name>Task 3: Docker Compose, Prisma migration, and manual end-to-end verification</name>

  <files>
    - `Dockerfile`
    - `Dockerfile.dev`
    - `docker-compose.yml`
    - `docker-compose.dev.yml`
    - `.env.example`
  </files>

  <action>
    Create Docker Compose configuration, Prisma migration, environment template, and prepare for manual verification.

    **1. `.env.example`:** (per D-18)
    ```
    # Discord
    DISCORD_TOKEN=your_discord_bot_token_here
    DISCORD_CLIENT_ID=your_application_client_id_here
    DISCORD_GUILD_ID=your_development_server_id_here

    # Database
    DATABASE_URL=postgresql://postgres:postgres@postgres:5432/language_partner_db
    POSTGRES_USER=postgres
    POSTGRES_PASSWORD=postgres
    POSTGRES_DB=language_partner_db

    # Redis
    REDIS_URL=redis://redis:6379

    # Environment
    NODE_ENV=development
    ```
    Note that the user must create `.env` from `.env.example` with real values. The `.env` file must NOT be committed (per D-18).

    **2. `Dockerfile`:** (multi-stage for production)
    - Stage 1 (build): node:22-alpine, copy package.json + prisma/, npm install, npx prisma generate, copy src/, npx tsc
    - Stage 2 (run): node:22-alpine, copy dist/ + node_modules/ + prisma/
    - CMD: ["node", "dist/index.js"]
    - HEALTHCHECK: NONE (per D-15 — healthcheck via container process status, not HTTP)

    **3. `Dockerfile.dev`:**
    - node:22-alpine
    - Install dependencies, copy all source
    - CMD: ["npx", "tsx", "watch", "src/index.ts"] (per D-20)
    - Note: node_modules excluded from bind mount in docker-compose.dev.yml (per D-21)

    **4. `docker-compose.yml`:** (base — per D-19, D-22)
    ```yaml
    services:
      postgres:
        image: postgres:17-alpine
        env_file: .env
        volumes:
          - postgres_data:/var/lib/postgresql/data
        healthcheck:
          test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
          interval: 10s
          timeout: 5s
          retries: 5
          start_period: 30s
        restart: unless-stopped

      redis:
        image: redis:7-alpine
        volumes:
          - redis_data:/data
        healthcheck:
          test: ["CMD", "redis-cli", "ping"]
          interval: 10s
          timeout: 3s
          retries: 5
          start_period: 10s
        restart: unless-stopped

      bot:
        build: .
        env_file: .env
        depends_on:
          postgres:
            condition: service_healthy
          redis:
            condition: service_healthy
        restart: unless-stopped

    volumes:
      postgres_data:
      redis_data:
    ```

    **5. `docker-compose.dev.yml`:** (dev overrides — per D-19)
    ```yaml
    services:
      bot:
        build:
          context: .
          dockerfile: Dockerfile.dev
        command: npx tsx watch src/index.ts
        volumes:
          - .:/app
          - /app/node_modules
        environment:
          - NODE_ENV=development
    ```

    **After creating files:**
    1. Run `npx prisma migrate dev --name init` to create the initial migration
    2. Verify migration file exists under prisma/migrations/
    3. Verify bot compiles: `npx tsc --noEmit`

    **The following manual verification steps must be completed by the user (checkpoint):**
  </action>

  <what-built>
    Complete Walking Skeleton for the Language Partner Bot:
    - Docker Compose with 3 services (postgres:17-alpine, redis:7-alpine, bot on node:22-alpine)
    - Dev Dockerfile with tsx watch hot-reload
    - Initial Prisma migration creating the User table
    - `.env.example` with all required environment variables documented
  </what-built>

  <how-to-verify>
    **Step 1: Environment Setup**
    1. Copy `.env.example` to `.env` and fill in:
       - DISCORD_TOKEN from Discord Developer Portal
       - DISCORD_CLIENT_ID from Discord Developer Portal OAuth2 page
       - DISCORD_GUILD_ID from your dev server (enable Developer Mode in Discord → right-click server → Copy ID)
       - Leave DATABASE_URL, REDIS_URL defaults (Docker Compose handles them)

    **Step 2: Docker Compose Startup**
    1. Run: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build`
    2. Verify all 3 containers are healthy: `docker compose ps`
       - postgres: status "healthy"
       - redis: status "healthy"
       - bot: status "running" (or "Up")
    3. Check bot logs: `docker compose logs bot`
       - Should show: "Logged in as LanguagePartnerBot#XXXX"
       - Should show: "Serving 1 guild" (or your dev server count)
       - No connection errors to PostgreSQL or Redis

    **Step 3: /ping Command**
    1. In your Discord dev server, type `/ping`
    2. Bot should respond with "Pong! 🏓 Latency: XXms" within 3 seconds

    **Step 4: /setup Command**
    1. Type `/setup`
    2. Select "Native Language" from first dropdown
    3. Select "Target Language" from second dropdown (your native should be excluded)
    4. Bot confirms: "✅ Configured! Native: English → Target: Spanish"
    5. Run `/setup` again — should start fresh with both selections (per D-06)

    **Step 5: Persistence Verification**
    1. Restart the bot: `docker compose restart bot`
    2. Wait for "ready" in logs
    3. Run `/setup` again — both selections should be available fresh (confirm overwrite works per D-06)

    **Step 6: /new Error Flow (per D-08)**
    1. Ensure you have a SECOND test user in the server who has NOT run /setup
    2. That user runs `/new` → sees: "⚠️ You need to configure your languages first!"

    **Step 7: Graceful Shutdown**
    1. Stop the bot: `docker compose stop --timeout=10 bot`
    2. Check logs: `docker compose logs bot`
    3. Should show: "[SIGTERM] Graceful shutdown started", "Discord client destroyed", "Prisma disconnected"

    **Step 8: All Services Healthy**
    1. Run: `docker compose ps`
    2. Verify all 3 services are in "Up" or "healthy" status
  </how-to-verify>

  <resume-signal>
    Type "approved" and describe any issues found during verification, or identify which step failed if blocking.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord API ↔ Bot WebSocket | Untrusted user interaction input crosses from Discord's API into bot process |
| Bot ↔ PostgreSQL | Database connection with credentials from DATABASE_URL env var |
| Bot ↔ Redis | Redis connection (provisioned but unused in Phase 1) |
| File system ↔ Bot process | .env file read at startup contains secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file — DISCORD_TOKEN | **High** | mitigate | .env excluded from git via .gitignore; token logged as [REDACTED]; Zod validates presence at startup and exits immediately if missing |
| T-01-02 | Tampering | SQL injection via Prisma | Medium | accept | Prisma ORM uses parameterized queries automatically; no raw SQL in Phase 1 codebase. Accept per ASVS L1 (medium-severity already mitigated by ORM choice) |
| T-01-03 | Elevation of Privilege | Env var injection at startup | Medium | mitigate | Zod schema validates every env var at process startup before any service connects. Invalid values cause process.exit(1) with clear error message |
| T-01-04 | Information Disclosure | Config exposure in logs | Low | accept | Bot logs do not print DISCORD_TOKEN or DATABASE_URL values. Prisma query logs in development mode may show data, but this is intentional debugging. Accept per ASVS L1 |
| T-01-05 | Tampering | Discord interaction injection | Low | accept | Discord validates interaction payloads via signed requests. Slash command options are string-typed by Discord schema. No freeform text input handled in Phase 1. Accept per ASVS L1 |
| T-01-06 | Tampering | Prisma version drift (accidental Prisma 7 upgrade) | Medium | mitigate | Pin @prisma/client and prisma to ^6.19.0 in package.json; verify with `npx prisma --version` after install |
| T-01-SC | Tampering | npm package installs | High | mitigate | RESEARCH.md contains complete Package Legitimacy Audit (all approved). Install runs in Docker build, pinned versions. No [SUS] or [SLOP] packages |
</threat_model>

<verification>

### Plan-Level Verification

| Check | Method | Pass Criteria |
|-------|--------|---------------|
| Automated tests pass | `npx vitest run --reporter=verbose` | All tests green |
| TypeScript compiles | `npx tsc --noEmit` | Zero errors |
| Prisma schema valid | `npx prisma validate` | Schema is valid |
| Linting passes | `npx biome check src/` | Zero errors |
| Docker Compose starts | `docker compose up -d` | All 3 containers healthy |
| Discord commands registered | Bot logs show command registration | All 3 commands registered |
| Graceful shutdown | `docker compose stop bot` | Cleanup logs visible |

### Nyquist Compliance

Every task produces either an `<automated>` verify command or a `<how-to-verify>` section. Wave 0 test infrastructure is created in Task 1 (first task of the plan) — no MISSING references exist at verification time.

</verification>

<success_criteria>

1. **User can run /setup** to select native and target language from dropdown menus, and the choice persists in PostgreSQL
2. **User settings survive restart** — verified by `docker compose restart bot`, re-checking /setup works
3. **/new with unconfigured user** returns error: "⚠️ You need to configure your languages first! Please run /setup"
4. **Docker Compose starts all services** (bot, PostgreSQL, Redis) with a single `docker compose up`
5. **All slash commands respond within 3 seconds** using deferReply() pattern
6. **Graceful shutdown** logs cleanup steps and exits cleanly

</success_criteria>

<output>
After completing all tasks, run `npx gsd-tools query commit "feat(01): implement phase 1 walking skeleton" --files .planning/phases/01-foundation-setup/*.md .planning/ROADMAP.md`

Then create the SUMMARY file: `.planning/phases/01-foundation-setup/01-01-SUMMARY.md`
</output>

