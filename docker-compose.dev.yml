services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: npx tsx watch src/index.ts
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
